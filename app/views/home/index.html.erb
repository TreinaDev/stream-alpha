<% if streamer_signed_in? %>
  <%= link_to 'Cadastrar Video', new_video_path %>
<% end %>

<div >
</div>
<div class="h-full w-full">
      <!--Nav-->
        <!--Left Col-->
        <div class="flex flex-col w-full justify-center lg:items-start overflow-y-hidden">
          <h1 class="my-4 text-3xl md:text-5xl mx-auto text-white opacity-75 font-bold leading-tight text-center md:text-left">
           Bem-vindo(a) a Plataforma
            <span class="bg-clip-text text-transparent bg-gradient-to-r from-green-400 via-pink-500 to-purple-500">
              GameStream
            </span>
          </h1>

        </div>
      </div>
    </div>



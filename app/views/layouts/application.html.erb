<!DOCTYPE html>
<html>
  <head>
    <title>Stream</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <div class="container">
      <% if admin_signed_in? %>
        <p><%= link_to "Sair", destroy_admin_session_path, method: :delete %></p>
      <% elsif streamer_signed_in? %>
        <p><%= link_to "Sair", destroy_streamer_session_path, method: :delete %></p>
      <% elsif client_signed_in? %>
        <p><%= link_to "Sair", destroy_client_session_path, method: :delete %></p>
      <% else %>
        <p><%= link_to "Entrar como Administrador", new_admin_session_path %></p>
        <p><%= link_to "Entrar como Streamer", new_streamer_session_path %></p>
        <p><%= link_to "Entrar como Assinante", new_client_session_path %></p>
      <% end %>
      <% if notice %>
        <p class="notice"><%= notice %></p>
      <% end %>
      <% if alert %>
        <p class="alert"><%= alert %></p>
      <% end %>

      <%= yield %>
    </div>
  </body>
</html>
